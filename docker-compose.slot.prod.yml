# Generated dynamically from registry/services.json. DO NOT EDIT.
services:
  auth:
    build:
      context: ../crm-auth
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    env_file:
      - ../crm-auth/.env.production
      - ./deploy/runtime/auth.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: server
    networks:
      - default
      - shared_backplane
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  auth-email-worker:
    build:
      context: ../crm-auth
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    command:
      - pnpm
      - worker:email
    env_file:
      - ../crm-auth/.env.production
      - ./deploy/runtime/auth.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: worker
    networks:
      - default
      - shared_backplane
    depends_on:
      auth:
        condition: service_healthy
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  auth-identity-outbox-worker:
    build:
      context: ../crm-auth
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    command:
      - pnpm
      - worker:identity-outbox
    env_file:
      - ../crm-auth/.env.production
      - ./deploy/runtime/auth.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: worker
    networks:
      - default
      - shared_backplane
    depends_on:
      auth:
        condition: service_healthy
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  auth-token-cleanup-worker:
    build:
      context: ../crm-auth
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    command:
      - pnpm
      - worker:cleanup
    env_file:
      - ../crm-auth/.env.production
      - ./deploy/runtime/auth.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: worker
    networks:
      - default
      - shared_backplane
    depends_on:
      auth:
        condition: service_healthy
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  collab:
    build:
      context: ../crm-collab
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    env_file:
      - ../crm-collab/.env.production
      - ./deploy/runtime/collab.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: server
    networks:
      - default
      - shared_backplane
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  collab-outbox-worker:
    build:
      context: ../crm-collab
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    command:
      - pnpm
      - worker:collab-outbox
    env_file:
      - ../crm-collab/.env.production
      - ./deploy/runtime/collab.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: worker
    networks:
      - default
      - shared_backplane
    depends_on:
      collab:
        condition: service_healthy
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  media:
    build:
      context: ../crm-media
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    env_file:
      - ../crm-media/.env.production
      - ./deploy/runtime/media.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: server
    networks:
      - default
      - shared_backplane
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  media-command-worker:
    build:
      context: ../crm-media
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    command:
      - pnpm
      - worker:media-commands
    env_file:
      - ../crm-media/.env.production
      - ./deploy/runtime/media.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: worker
    networks:
      - default
      - shared_backplane
    depends_on:
      media:
        condition: service_healthy
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  media-quarantine-scan-worker:
    build:
      context: ../crm-media
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    command:
      - pnpm
      - worker:quarantine-scan
    env_file:
      - ../crm-media/.env.production
      - ./deploy/runtime/media.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: worker
    networks:
      - default
      - shared_backplane
    depends_on:
      media:
        condition: service_healthy
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  frontend:
    build:
      context: ../crm-frontend
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        NGINX_IMAGE: ${NGINX_IMAGE:-nginx:1.27-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    restart: always
    ports:
      - 127.0.0.1:${FRONTEND_SLOT_HOST_PORT:?FRONTEND_SLOT_HOST_PORT is required}:80
    depends_on:
      api-gateway:
        condition: service_started
    networks:
      - default
  marketing:
    build:
      context: ../crm-marketing
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_VERSION: ${PNPM_VERSION:-11.1.1}
    init: true
    restart: always
    env_file:
      - ../crm-marketing/.env.production
      - ./deploy/runtime/marketing.${APP_SLOT:?APP_SLOT is required}.env
    environment:
      CONTAINER_MODE: server
    networks:
      - default
      - shared_backplane
    volumes:
      - /opt/cima/secrets:/opt/cima/secrets:ro
  api-gateway:
    image: ${KRAKEND_IMAGE:-devopsfaith/krakend:2.9}
    restart: always
    ports:
      - 127.0.0.1:${GATEWAY_SLOT_HOST_PORT:?GATEWAY_SLOT_HOST_PORT is required}:8080
    volumes:
      - ./deploy/runtime/krakend.${APP_SLOT:?APP_SLOT is required}.json:/etc/krakend/krakend.json:ro
    networks:
      - default
networks:
  shared_backplane:
    external: true
    name: crm-shared-backplane
