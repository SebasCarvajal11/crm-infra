# This directory holds sops-encrypted secret files for production.
# Files here are committed to the repo in encrypted form.
#
# To encrypt:   sops -e ../.env.docker > docker.env.enc
# To decrypt:   sops -d docker.env.enc > ../.env.docker
#
# Requires the SOPS_AGE_KEY secret in CI or age key locally.
